ALTER TABLE ITEMMETACLIENTE
ADD REPLICAR DOMAIN_SNNULL;

SET TERM ^ ;

CREATE OR ALTER TRIGGER TRIGGER_METACLIENTE_BIU FOR ITEMMETACLIENTE
ACTIVE BEFORE INSERT OR UPDATE POSITION 0
AS
BEGIN
    NEW.REPLICAR = 'S';
END^

SET TERM ; ^

SET TERM ^ ;

CREATE OR ALTER TRIGGER TRIGGER_METACLIENTE_REPLICAR FOR ITEMMETACLIENTE
ACTIVE BEFORE INSERT OR UPDATE POSITION 99
AS
BEGIN
    NEW.REPLICAR = 'N';
END^

SET TERM ; ^


SET TERM ^ ;

CREATE OR ALTER trigger analytics_bi_metacliente for itemmetacliente
active before insert or update position 10
AS
DECLARE VARIABLE TERMINAL INTEGER;
DECLARE VARIABLE SCRIPT VARCHAR(4096);
DECLARE VARIABLE DISTRIBUIDOR VARCHAR(18);
declare variable CLIENTE VARCHAR(18);
BEGIN
    FOR
        SELECT TERMINAL.CODIGO, FILIAL.CNPJ, CLIFOR.CNPJ
        FROM TERMINAL
        INNER JOIN META ON META.CODIGO = NEW.META
        INNER JOIN FILIAL ON FILIAL.CODIGO = TERMINAL.FILIAL AND TERMINAL.FILIAL = META.FILIAL
        INNER JOIN CLIFOR ON CLIFOR.CODIGO = NEW.CLIENTE
        WHERE ((NEW.REPLICAR = 'S') AND (TERMINAL.ATIVO = 'S') AND (TERMINAL.TIPOREPLICACAO = 'B') AND (TERMINAL.ENVIARCARGA = 'N'))
        OR ((NEW.REPLICAR = 'C') AND (TERMINAL.ATIVO = 'S') AND (TERMINAL.TIPOREPLICACAO = 'B') AND (TERMINAL.ENVIARCARGA = 'S'))
        INTO :TERMINAL, :DISTRIBUIDOR, :CLIENTE
    DO BEGIN
            SCRIPT = 'EXECUTE PROCEDURE '
                || 'SET_BI_METACLIENTE'
                || '('
                || '''' ||:DISTRIBUIDOR || ''''
                || ', ' || NEW.META
                || ', ' || '''' || :CLIENTE || ''''
                || ', ' || NEW.VALOR
                || ', ' || NEW.QTDE
                || ', ' || NEW.PESOBRUTO
                || ', ' || NEW.VOLUME
                || ')';
            INSERT INTO SCRIPTSQL(SCRIPTSQL.TERMINAL, SCRIPTSQL.SCRIPT) VALUES (:TERMINAL, :SCRIPT);
    END
END^

SET TERM ; ^

SET TERM ^ ;

CREATE OR ALTER trigger analytics_bi_del_metacliente for itemmetacliente
active after delete position 0
AS
DECLARE VARIABLE TERMINAL INTEGER;
DECLARE VARIABLE SCRIPT VARCHAR(4096);
DECLARE VARIABLE DISTRIBUIDOR VARCHAR(18);
declare variable CLIENTE VARCHAR(18);
BEGIN
    FOR
        SELECT TERMINAL.CODIGO, FILIAL.CNPJ, CLIFOR.CNPJ
        FROM TERMINAL
        INNER JOIN META ON META.CODIGO = OLD.META
        INNER JOIN FILIAL ON FILIAL.CODIGO = TERMINAL.FILIAL AND TERMINAL.FILIAL = META.FILIAL
        INNER JOIN CLIFOR ON CLIFOR.CODIGO = OLD.CLIENTE
        WHERE TERMINAL.TIPOREPLICACAO = 'B'
        INTO :TERMINAL, :DISTRIBUIDOR, :CLIENTE
    DO BEGIN
        SCRIPT = 'EXECUTE PROCEDURE '
              || 'DEL_BI_METACLIENTE'
              || '(' || OLD.META
              || ', ' || '''' || :DISTRIBUIDOR || ''''
              || ', ' || '''' || :CLIENTE || ''''
              || ')';
        INSERT INTO SCRIPTSQL(SCRIPTSQL.TERMINAL, SCRIPTSQL.SCRIPT) VALUES (:TERMINAL, :SCRIPT);
    END
END^

SET TERM ; ^

